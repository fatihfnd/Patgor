[[headers]]
  for = "/*"
  [headers.values]
    # Required for SharedArrayBuffer / WASM multi-threading (OpenCV.js)
    Cross-Origin-Opener-Policy = "same-origin"
    Cross-Origin-Embedder-Policy = "require-corp"

[build]
  publish = "."
